<% include ../partials/header %>

<div class="container">
  <div class="row">
    <div class="col-md-4">
      <div class="img-thumbnail p-0">
        <h1><%= user.firstName + " " + user.lastName %></h1>
        <img src="<%= user.avatar %>" alt="user profile image">
        <a href="mailto:<%= user.email %>"><%= user.email %></a>
      </div>
      Lorem ipsum dolor sit amet, consectetur adipisicing elit. Eos eligendi est, beatae. 
      Qui beatae aperiam saepe, enim recusandae harum molestias vero voluptatum earum, sapiente, 
      alias cumque quisquam quibusdam fugiat tenetur.
    </div>
    <div class="col-md-8">
      <% campgrounds.forEach(function(campground) { %>
          <h3><%= user.firstName %>'s campgrounds!</h3>
          <div class="img-thumbnail p-0 userImgThumbnail">
            <img src="<%= campground.image %>">
            <div class="px-2 pt-2">
              <h5 class="float-right">$<%= campground.price %>/night</h5>
              <a href="/campgrounds/<%=campground.id %>"><h5><%= campground.name %></h5></a>
              <p><%= campground.description %></p>
            </div>
          </div>
      <% }); %>
    </div>
  </div>
</div>


<% include ../partials/footer %>